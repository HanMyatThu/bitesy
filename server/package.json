{
  "name": "apis_for_bitesy",
  "version": "1.0.0",
  "description": "REST API for Bitesy App",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && tsc-alias",
    "start": "node ./dist",
    "dev": "tsnd --respawn -r tsconfig-paths/register --transpile-only ./src/index.ts"
  },
  "author": "draz",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "mongoose": "^8.5.3",
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.2.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2"
  }
}
